name: Test Formula
on:
  push:
    branches: [main]
    paths: ['Formula/**']
  pull_request:

jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - uses: Homebrew/actions/setup-homebrew@master
      - name: Tap local repo
        run: |
          mkdir -p $(brew --repository)/Library/Taps/scotts
          ln -s $GITHUB_WORKSPACE $(brew --repository)/Library/Taps/scotts/homebrew-dune3d
      - name: Install from source
        run: brew install --build-from-source scotts/dune3d/dune3d
      - name: Verify binary
        run: otool -L $(brew --prefix)/bin/dune3d
      - name: Test
        run: brew test scotts/dune3d/dune3d
      - name: Audit
        run: brew audit --formula scotts/dune3d/dune3d
